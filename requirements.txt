yamllint # Command-Line Linters.
PyYAML # To read YAML files. YAML parser and emitter.
psycopg2-binary # a standalone and self-contained distribution of the psycopg2 library
sqlalchemy # this ORM will transform the python objects into SQL tables
pandas # pandas dataframe
nbformat # to save in ipynb
missingno # Visualising Missing Data
plotly.express # Visualising 
pip install scikit-learn # for uploading df to your postgres
pip install tabula-py # read tables in a PDF
pip install jpype1 # needed for my s3 extraction tabula
python-decouple # to use with sensitive information i.e. passwords

